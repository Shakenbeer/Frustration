package com.shakenbeer.frustration

enum class Chapter(val display: String) {
    CONTENT("Content"),
    INTRO("1. Intro"),
    SUSPEND_FUNC("2. Suspend function"),
    SCOPE("3. Job and Scope"),
    CANCEL("4. Run and cancel a coroutine"),
    JOIN("5. Builders: wait with join"),
    LAZY("6. Builders: delay with lazy"),
}